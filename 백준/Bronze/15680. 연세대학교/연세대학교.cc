#include <string>
#include <vector>
#include <stdlib.h>
#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int n = 0;

    cin >> n;

    if (n == 0)
    {
        cout << "YONSEI" << '\n';
    }
    else if (n == 1)
    {
        cout << "Leading the Way to the Future" << '\n';
    }

    return 0;
}